import 'package:flutter/material.dart';

class PresentSimpl extends StatelessWidget {
  const PresentSimpl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
